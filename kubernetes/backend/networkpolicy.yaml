# apiVersion: networking.k8s.io/v1
# kind: NetworkPolicy
# metadata:
#   name: backend-service-policy
#   namespace: default
# spec:
#   podSelector:
#     matchLabels:
#       app: backend-service
#   policyTypes:
#   - Ingress
#   ingress:
#   # Allow traffic from Gateway VPC CIDR
#   - from:
#     - ipBlock:
#         cidr: 10.0.0.0/16
#     ports:
#     - protocol: TCP
#       port: 8080
#   # Allow traffic from within the same namespace (for health checks)
#   - from:
#     - podSelector: {}
#     ports:
#     - protocol: TCP
#       port: 8080
